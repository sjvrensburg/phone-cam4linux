The Typst side of MiTeX (https://github.com/mitex-rs/mitex), `packages/mitex/specs/`
at tag 0.2.6, Apache-2.0: the scope of LaTeX command handlers the converted math is
evaluated in. The converter itself is the `mitex` crate; `mitex.typ` and the wasm
plugin are not needed since the conversion happens in Rust.
